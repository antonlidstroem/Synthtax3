<Window x:Class="Synthtax.Admin.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Synthtax Admin — Användarhantering"
        Width="960" Height="640"
        MinWidth="800" MinHeight="500"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource PrimaryBrush}">

  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>  <!-- Header -->
      <RowDefinition Height="*"/>     <!-- Content -->
      <RowDefinition Height="Auto"/>  <!-- Status bar -->
    </Grid.RowDefinitions>

    <!-- ── Header ─────────────────────────────────────────────────────── -->
    <Grid Grid.Row="0" Margin="0,0,0,16">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <TextBlock Text="SYNTHTAX" FontSize="20" FontWeight="Bold"
                   Foreground="{StaticResource AccentBrush}"/>
        <TextBlock Text=" Admin" FontSize="20" Foreground="{StaticResource TextBrush}"/>
      </StackPanel>

      <Button Grid.Column="1" Content="Uppdatera"
              Style="{StaticResource BaseButton}"
              Command="{Binding RefreshCommand}"
              Margin="0,0,10,0"/>
      <Button Grid.Column="2" Content="Logga ut"
              Style="{StaticResource DangerButton}"
              Command="{Binding LogoutCommand}"/>
    </Grid>

    <!-- ── Main content ───────────────────────────────────────────────── -->
    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="280"/>
      </Grid.ColumnDefinitions>

      <!-- Användarlista -->
      <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8"
              Margin="0,0,16,0">
        <DataGrid ItemsSource="{Binding Users}"
                  SelectedItem="{Binding SelectedUser}"
                  Margin="0">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Användarnamn"  Binding="{Binding UserName}"    Width="*"/>
            <DataGridTextColumn Header="E-post"         Binding="{Binding Email}"       Width="*"/>
            <DataGridTextColumn Header="Roll"           Binding="{Binding DisplayRoles}" Width="120"/>
            <DataGridCheckBoxColumn Header="Låst"       Binding="{Binding IsLocked}"    Width="60"/>
          </DataGrid.Columns>
        </DataGrid>
      </Border>

      <!-- Sidopanel: detaljer + åtgärder -->
      <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}"
              CornerRadius="8" Padding="16">
        <StackPanel>
          <TextBlock Text="Åtgärder" FontSize="15" FontWeight="SemiBold"
                     Foreground="{StaticResource TextBrush}" Margin="0,0,0,16"/>

          <!-- Vald användare -->
          <TextBlock Text="Vald användare:" FontSize="11"
                     Foreground="{StaticResource MutedBrush}" Margin="0,0,0,4"/>
          <TextBlock Text="{Binding SelectedUser.UserName, FallbackValue='—'}"
                     FontSize="14" FontWeight="SemiBold"
                     Foreground="{StaticResource TextBrush}" Margin="0,0,0,20"/>

          <!-- Lås/Lås upp -->
          <Button Content="Lås / Lås upp konto"
                  Style="{StaticResource BaseButton}"
                  Command="{Binding ToggleLockCommand}"
                  IsEnabled="{Binding HasSelection}"
                  HorizontalAlignment="Stretch"
                  Margin="0,0,0,10"/>

          <!-- Ge/ta admin-roll (bara superadmin) -->
          <Button Content="Ge / Ta Admin-roll"
                  Style="{StaticResource BaseButton}"
                  Command="{Binding ToggleAdminCommand}"
                  IsEnabled="{Binding IsSuperAdmin}"
                  HorizontalAlignment="Stretch"
                  Margin="0,0,0,10"/>

          <!-- Radera -->
          <Button Content="Radera användare"
                  Style="{StaticResource DangerButton}"
                  Command="{Binding DeleteUserCommand}"
                  IsEnabled="{Binding HasSelection}"
                  HorizontalAlignment="Stretch"
                  Margin="0,0,0,24"/>

          <!-- Återställ lösenord -->
          <TextBlock Text="Nytt lösenord:" FontSize="11"
                     Foreground="{StaticResource MutedBrush}" Margin="0,0,0,6"/>
          <TextBox Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}"
                   Margin="0,0,0,8"/>
          <Button Content="Återställ lösenord"
                  Style="{StaticResource BaseButton}"
                  Command="{Binding ResetPasswordCommand}"
                  IsEnabled="{Binding HasSelection}"
                  HorizontalAlignment="Stretch"/>
        </StackPanel>
      </Border>
    </Grid>

    <!-- ── Status bar ─────────────────────────────────────────────────── -->
    <Border Grid.Row="2" Margin="0,12,0,0">
      <TextBlock Text="{Binding StatusMessage}"
                 Foreground="{StaticResource MutedBrush}"
                 FontSize="12"/>
    </Border>
  </Grid>
</Window>
